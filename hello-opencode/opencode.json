{
  "$schema": "https://opencode.ai/config.json",
  "permission": "allow",
  "command": {
    "generate-docs": {
      "template": "Explore the application at $1 and generate a user guide for the $2 feature. Save the output to docs/$2-guide.md and include screenshots for each step.",
      "description": "Generate user docs with Playwright screenshots",
      "agent": "doc-agent"
    }
  },
  "agent": {
    "doc-agent": {
      "prompt": "{file:.opencode/agents/doc-agent.txt}"
    }
  },
  "mcp": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"],
      "enabled": true
    }
  }

}
