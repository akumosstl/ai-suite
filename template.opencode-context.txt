# 🧠 PROJECT MEMORY (Java/Angular)

## 🎯 General Context
- **Project:** [Project Name]
- **Backend:** Java 17+, Spring Boot (Spring Security, Spring Data JPA).
- **Frontend:** Angular (v16+), TypeScript, RxJS.
- **Database:** H2 Database (Console available at `/h2-console`).
- **Architecture:** REST API (JSON).

## 🛠 Architecture Decisions & Patterns
- **Standardization:** DTOs for input/output; Clean Controllers.
- **Service Layer:** All business logic must reside in `@Service` classes.
- **Angular:** Components following the "Smart and Dumb Components" pattern.
- **H2:** Since it's an in-memory DB, keep `data.sql` and `schema.sql` updated to avoid losing data between restarts.

## 📍 Current State (Last Session: [DATE])
- **Where we left off:** [Describe the last task here]
- **Backend Status:** [e.g., Repositories and Services created, Controller pending]
- **Frontend Status:** [e.g., Angular Service created, form pending]
- **Critical Pending Issues:** [e.g., CORS error when connecting Angular to Spring]

## 📝 Style & Coding Rules
- **Java:** Follow Google Java Style conventions; use Lombok to reduce boilerplate.
- **Angular:** TS Interfaces for model typing; use `AsyncPipe` whenever possible.
- **Endpoints:** Plural and kebab-case pattern (e.g., `/api/v1/user-profiles`).

## 🚀 Next Steps (Immediate Backlog)
1. [ ] Step 1
2. [ ] Step 2
3. [ ] Step 3
